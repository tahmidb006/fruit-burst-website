// tailwind.config.js
module.exports = {
    purge: [],
    darkMode: false, // Set to 'media' or 'class' if you want dark mode support
    theme: {
        extend: {},
    },
    variants: {},
    plugins: [require('daisyui')],
};
